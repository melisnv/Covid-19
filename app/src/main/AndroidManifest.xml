<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.melisnurverir.covid_19">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covid19">
        <activity android:name=".UploadActivity"></activity>
        <activity android:name=".ShareStoryActivity" />
        <activity android:name=".SignInUpActivity" />
        <activity android:name=".Articles" />
        <activity android:name=".TurkeyData" />
        <activity android:name=".GreeceData" />
        <activity android:name=".FranceData" />
        <activity android:name=".ItalyData" />
        <activity android:name=".GermanyData" />
        <activity android:name=".Statistics" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>